---
- name: Verify Team Role
  hosts: localhost
  connection: local
  gather_facts: false
  
  vars:

  tasks:
    - name: Import varaible
      ansible.builtin.include_role: 
        name: ../roles/verify_team_role
